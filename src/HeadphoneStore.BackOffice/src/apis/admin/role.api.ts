import { URL } from ".."

export const BASE_URL = `${URL}/Role`

export const ROLE_API = {
  CREATE: `${BASE_URL}/create`,
  UPDATE: `${BASE_URL}`,
  DELETE: `${BASE_URL}`,
  ALL: `${BASE_URL}/all`,
}
